# Agent Rules

## 기록

- 작업이 끝나면 **계획과 결과**를 남긴다.
- 계획·설계는 `docs/` (또는 기존 계획 MD 갱신).
- 구현 완료·검증 결과는 `reports/`에 남긴다.
- 추측으로 쓰지 말고, 실제로 한 일과 확인한 결과만 적는다.

## 코드 패턴

- `d:\harmonic_hear\` 작업 시 스타일·구조는 **기존 앱 파일**(`app/`, `src/`)을 보고 맞춘다.
- 패턴을 파일명만으로 추측하지 않는다. 필요한 파일은 읽고 따른다.

## 플랫폼

- Android 우선. iOS 전용 API·설정을 제안하거나 넣지 않는다.
