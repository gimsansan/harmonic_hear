# 미사용 코드·의존성 정리 계획

날짜: 2026-08-10

## 목표

런타임에 쓰이지 않는 코드·설정 복제본·직접 import 없는 의존성을 제거해 혼선을 줄인다.

## 범위

| 대상 | 조치 |
|------|------|
| `src/audio/keyzoneUtils.ts` | 삭제 (호출처 없음) |
| `src/constants/theme.ts`의 `KEYZONE` | 삭제 (위 파일 전용) |
| `files/package.json`, `package-lock.json`, `app.json` | 삭제 (루트 설정 복제·Jest 충돌 이력) |
| `package.json`의 `reset-project`, `ios` | 이미 없음 / Android 전제에 맞게 유지 |
| `@react-navigation/bottom-tabs` | dependencies에서 제거 |
| `expo-audio` | dependencies·`app.json` plugins에서 제거 (재생은 `react-native-audio-api`) |
| `expo-screen-orientation` | dependencies에서 제거 (코드 미사용) |

## 유지

- `files/` 명세·계획 MD (`하모니튠_통합_구현명세서.md` 등)
- `expo-font` / `expo-asset` / `expo-linking` / `@react-navigation/native` (직접 import 없어도 expo-router 등에서 사용 가능)
- `docs/`, `reports/`
