# 작업 기록 — 1단계: P0 결함 수정 + 회귀 테스트

| | |
|---|---|
| **작업 기간** | 2026-08-07 |
| **범위** | `docs/앱_개선_제안서.md` **1단계** (P0 결함 4건 + 회귀 테스트) |
| **상태** | **작업 완료 · 이 문서 마감** |
| **다음 기록** | `reports/작업기록_2단계_측정타당도.md` (2단계 착수 시 생성) |

`docs/앱_개선_제안서_쉬운버전.md` 생성 **이후**의 작업 기록입니다.
사용자 지시 → 수행 내용 → 코드 수정 전후를 순서대로 남깁니다.

> **문서 운영 규칙 (사용자 결정, 지시 13)**
> 작업 기록은 **단계별로 파일을 나눕니다.** 한 파일이 무한정 길어지는 것을 막기 위함입니다.
> 이 문서는 1단계에서 마감하며, 2단계 지시부터는 새 파일에 기록합니다.

관련 문서
- [`docs/앱_개선_제안서.md`](../docs/앱_개선_제안서.md) — 상세 제안서
- [`docs/앱_개선_제안서_쉬운버전.md`](../docs/앱_개선_제안서_쉬운버전.md) — 쉬운 설명 버전 (**이 기록의 기준점**)
- [`reports/p0-fixes-2026-08-07.md`](./p0-fixes-2026-08-07.md) — P0 수정 상세
- [`reports/p0-regression-tests-2026-08-07.md`](./p0-regression-tests-2026-08-07.md) — 회귀 테스트 상세
- [`docs/실기기_확인_체크리스트.md`](../docs/실기기_확인_체크리스트.md) — 사람이 확인해야 할 항목

---

## 0. 기준점 — 여기까지가 이미 아시던 내용

| # | 지시 | 결과물 |
|---|------|--------|
| 1 | "이 앱을 파악 후 개선사항을 제안해줘 … md로 생성해줘" | `docs/앱_개선_제안서.md` (신규) |
| 2 | "핵심 추린 내용이 어려운데 쉽게 설명해줄래?" | 대화로만 설명. 파일 변경 없음 |
| 3 | "쉬운 버전도 md 파일로 만들어줘" | `docs/앱_개선_제안서_쉬운버전.md` (신규) |

**이 시점까지 앱 코드는 한 줄도 바뀌지 않았습니다.** 문서 2개만 추가됐습니다.

아래부터가 새로 기록하는 내용입니다.

---

## 지시 4 — "1부 4개 문제 고쳐줘"

쉬운버전 문서 1부에 적힌 P0 결함 4건을 수정했습니다.

### 4-1. 수정: 답변 중복 제출 차단 (P0-1)

**문제.** 답변 후 상태가 `'answered'`인데 버튼이 활성이고 핸들러도 통과했습니다.
피드백으로 정답을 본 뒤 같은 버튼을 반복 탭하면 시행 수·정답 수·난이도가 무제한 누적됐습니다.

**파일:** `app/training.tsx`

**전**
```tsx
const handleAnswer = useCallback(
  (userThinksHigher: boolean) => {
    if (gameState !== 'waiting' && gameState !== 'answered') return;
    ...
```
```tsx
<AnswerButtons
  disabled={gameState === 'playing' || gameState === 'idle'}
  onAnswer={handleAnswer}
/>
```

**후**
```tsx
const handleAnswer = useCallback(
  (userThinksHigher: boolean) => {
    if (!canSubmitAnswer(gameState)) return;   // 'waiting'만 통과
    ...
```
```tsx
{/* 답변 — 'waiting'(재생 완료 후 미답변)일 때만 받는다 */}
<AnswerButtons
  disabled={!canSubmitAnswer(gameState)}
  onAnswer={handleAnswer}
/>
```

> `canSubmitAnswer`는 **지시 6**에서 도입한 함수입니다.
> 지시 4 시점에는 `gameState !== 'waiting'` / `gameState === 'waiting'`을 직접 썼고,
> 지시 6에서 순수 함수로 추출하며 이 형태가 됐습니다. 판정 결과는 동일합니다.

---

### 4-2. 수정: 다시 듣기 / 다음 문제 분리 (P0-2)

**문제.** 재생 버튼이 상태와 무관하게 항상 `prepareRound()`를 호출했고,
`StaircaseEngine.prepareRound()`는 매번 `Math.random() > 0.5`로 방향을 새로 정했습니다.
→ "다시 듣기"가 정답을 재추첨했고, 답하지 않고 재생만 반복해 어려운 문제를 회피할 수 있었습니다.

**파일:** `app/training.tsx`

**전** — 하나의 핸들러가 추첨과 재생을 모두 담당
```tsx
const handlePlaySound = useCallback(async () => {
  if (gameState === 'playing') return;

  if (!isSessionActive) {
    await handleStartSession();
  }

  const audioReady = await audioEngine.ensureRunning();
  if (!audioReady) {
    console.warn('[Training] AudioContext를 시작할 수 없습니다.');
    return;
  }

  clearTimers();
  setGameState('playing');
  setFeedback(null);

  const roundState = sessionManager.prepareRound();   // ← 항상 재추첨
  setCentsDifference(roundState.centsDifference);
  setBaseFreq(roundState.baseFreq);

  const roundBaseFreq = roundState.baseFreq;
  const targetFreq = roundState.targetFreq;
  const duration = AUDIO.TONE_DURATION;
  const gap = AUDIO.GAP_DURATION;

  setActiveSound('A');
  void audioEngine.playTone(roundBaseFreq, 0, duration, soundMode);

  addTimer(() => setActiveSound('none'), duration * 1000);

  addTimer(() => {
    setActiveSound('B');
    void audioEngine.playTone(targetFreq, 0, duration, soundMode);
  }, (duration + gap) * 1000);

  addTimer(() => {
    setActiveSound('none');
    setGameState('waiting');
  }, (duration + gap + duration) * 1000);
}, [gameState, isSessionActive, soundMode, audioEngine, sessionManager,
    clearTimers, addTimer, handleStartSession]);
```

**후** — 재생을 라운드 결정에서 분리, 호출부를 둘로 나눔
```tsx
/**
 * A → 대기 → B 재생 시퀀스.
 *
 * 이미 정해진 라운드의 주파수를 그대로 재생합니다.
 * 방향(정답) 추첨은 이 함수가 하지 않습니다 — 호출부에서 결정합니다.
 */
const runPlaybackSequence = useCallback(
  (roundBaseFreq: number, targetFreq: number) => {
    const duration = AUDIO.TONE_DURATION;
    const gap = AUDIO.GAP_DURATION;

    clearTimers();
    setGameState('playing');

    setActiveSound('A');
    void audioEngine.playTone(roundBaseFreq, 0, duration, soundMode);

    addTimer(() => setActiveSound('none'), duration * 1000);

    addTimer(() => {
      setActiveSound('B');
      void audioEngine.playTone(targetFreq, 0, duration, soundMode);
    }, (duration + gap) * 1000);

    // 답변 대기 — 반응 시간은 이 시점부터 측정
    addTimer(() => {
      setActiveSound('none');
      sessionManager.openResponseWindow();
      setGameState('waiting');
    }, (duration + gap + duration) * 1000);
  },
  [soundMode, audioEngine, sessionManager, clearTimers, addTimer],
);

/** 새 문제 — 방향을 새로 추첨하고 재생 */
const handleNextRound = useCallback(async () => {
  if (!canPressPlay(gameState)) return;

  if (!isSessionActive) {
    await handleStartSession();
  }

  const audioReady = await audioEngine.ensureRunning();
  if (!audioReady) {
    console.warn('[Training] AudioContext를 시작할 수 없습니다.');
    return;
  }

  setFeedback(null);

  const roundState = sessionManager.prepareRound();
  setCentsDifference(roundState.centsDifference);
  setBaseFreq(roundState.baseFreq);

  runPlaybackSequence(roundState.baseFreq, roundState.targetFreq);
}, [gameState, isSessionActive, audioEngine, sessionManager,
    handleStartSession, runPlaybackSequence]);

/** 다시 듣기 — 같은 문제를 그대로 재생 (방향 재추첨 없음) */
const handleReplay = useCallback(async () => {
  if (!isReplayPress(gameState)) return;

  const audioReady = await audioEngine.ensureRunning();
  if (!audioReady) {
    console.warn('[Training] AudioContext를 시작할 수 없습니다.');
    return;
  }

  // prepareRound를 호출하지 않으므로 targetFreq·isHigher가 유지됨
  const roundState = sessionManager.getStaircaseState();
  sessionManager.countReplay();

  runPlaybackSequence(roundState.baseFreq, roundState.targetFreq);
}, [gameState, audioEngine, sessionManager, runPlaybackSequence]);

/** 재생 버튼: 답변 대기 중이면 '다시 듣기', 그 외에는 '새 문제' */
const handlePlayPress = isReplayPress(gameState)
  ? handleReplay
  : handleNextRound;
```

**버튼 문구도 상태별로 분기**

전
```tsx
<Text style={styles.playButtonText}>
  {gameState === 'playing'
    ? '🔊 소리 감상 중...'
    : gameState === 'answered'
      ? '▶️ 다음 소리 듣기 (A → B)'
      : '▶️ 소리 듣기 (A → B)'}
</Text>
```

후
```tsx
<Text style={styles.playButtonText}>
  {PLAY_BUTTON_LABEL[gameState]}
</Text>
```

| 상태 | 전 | 후 |
|------|-----|-----|
| `idle` | ▶️ 소리 듣기 (A → B) | ▶️ 소리 듣기 (A → B) |
| `playing` | 🔊 소리 감상 중... | 🔊 소리 감상 중... |
| `waiting` | ▶️ 소리 듣기 (A → B) | **🔁 다시 듣기 (A → B)** |
| `answered` | ▶️ 다음 소리 듣기 (A → B) | ▶️ 다음 소리 듣기 (A → B) |

**부가: 다시 듣기 횟수 기록** — `src/training/SessionManager.ts`

전
```ts
export interface TrialRecord {
  trialNumber: number;
  isCorrect: boolean;
  /** 반응 시간 (밀리초) */
  reactionTimeMs: number;
  centsDifference: number;
  isHigher: boolean;
  timestamp: number;
}
```

후
```ts
export interface TrialRecord {
  trialNumber: number;
  isCorrect: boolean;
  /**
   * 반응 시간 (밀리초).
   * 자극 재생이 끝나 답변이 가능해진 시점부터 측정합니다.
   * 측정 시작 전에 답변이 들어온 경우 0.
   */
  reactionTimeMs: number;
  /** 이 시행에서 '다시 듣기'를 누른 횟수. 구버전 세션에는 없을 수 있음 */
  replayCount?: number;
  centsDifference: number;
  isHigher: boolean;
  timestamp: number;
}
```

`replayCount`를 **optional**로 둔 이유: 이전에 저장된 세션 데이터에는 이 필드가 없으므로,
필수로 만들면 기존 기록을 읽을 때 타입이 어긋납니다.

---

### 4-3. 수정: 반응 시간 측정 시점 이동 (P0-3)

**문제.** `prepareRound()`에서 시계를 잡아, A(1.0s) + 갭(0.5s) + B(1.0s) = **2500ms가 항상 포함**됐습니다.
통계 화면의 "평균 반응 시간"은 구조적으로 3000ms 미만이 나올 수 없었습니다.

**파일:** `src/training/SessionManager.ts`

**전**
```ts
private roundStartTime: number = 0;

/**
 * 새 라운드를 준비합니다.
 * 반응 시간 측정을 시작합니다.
 */
prepareRound(): StaircaseState {
  this.roundStartTime = Date.now();          // ← 재생 시작 시점
  return this.staircaseEngine.prepareRound();
}

submitAnswer(userThinksHigher: boolean): TrialResult {
  const reactionTimeMs = Date.now() - this.roundStartTime;
  const currentState = this.staircaseEngine.getState();
  const result = this.staircaseEngine.submitAnswer(userThinksHigher);

  this.trials.push({
    trialNumber: this.trials.length + 1,
    isCorrect: result.isCorrect,
    reactionTimeMs,
    centsDifference: currentState.centsDifference,
    isHigher: currentState.isHigher,
    timestamp: Date.now(),
  });
```

**후**
```ts
/** 답변 가능 시점(= B 재생 종료)의 타임스탬프. 0이면 아직 열리지 않음 */
private responseWindowOpenedAt: number = 0;
/** 현재 라운드에서 '다시 듣기'를 누른 횟수 */
private replayCount: number = 0;

/**
 * 새 라운드를 준비합니다. (B의 방향을 새로 추첨)
 *
 * 반응 시간 측정은 여기서 시작하지 않습니다.
 * 자극 재생이 끝난 뒤 `openResponseWindow()`가 호출되는 시점부터 잽니다.
 */
prepareRound(): StaircaseState {
  this.responseWindowOpenedAt = 0;
  this.replayCount = 0;
  return this.staircaseEngine.prepareRound();
}

/**
 * 답변 가능 시점을 표시합니다. (= A→B 재생이 끝나 답변 버튼이 활성화될 때)
 * 이 시점부터 반응 시간을 측정합니다.
 *
 * '다시 듣기'로 재재생한 경우에도 마지막 재생 종료 시점 기준으로 갱신됩니다.
 */
openResponseWindow(): void {
  this.responseWindowOpenedAt = Date.now();
}

/**
 * 현재 라운드의 '다시 듣기' 횟수를 1 증가시킵니다.
 * 방향(정답)은 바뀌지 않습니다.
 */
countReplay(): void {
  this.replayCount += 1;
}

submitAnswer(userThinksHigher: boolean): TrialResult {
  const now = Date.now();
  const reactionTimeMs =
    this.responseWindowOpenedAt > 0 ? now - this.responseWindowOpenedAt : 0;
  const currentState = this.staircaseEngine.getState();
  const result = this.staircaseEngine.submitAnswer(userThinksHigher);

  this.trials.push({
    trialNumber: this.trials.length + 1,
    isCorrect: result.isCorrect,
    reactionTimeMs,
    replayCount: this.replayCount,
    centsDifference: currentState.centsDifference,
    isHigher: currentState.isHigher,
    timestamp: now,
  });

  // 답변이 끝난 라운드이므로 측정 창을 닫는다 (중복 제출 방지 보조)
  this.responseWindowOpenedAt = 0;
```

**세션 시작 시 초기화도 추가**

전
```ts
startSession(): void {
  this.startedAt = Date.now();
  this.trials = [];
  this.isActive = true;
  this.staircaseEngine.reset(this.config.baseFreq);
  this.minCentsAchieved = this.staircaseEngine.getState().centsDifference;
}
```

후
```ts
startSession(): void {
  this.startedAt = Date.now();
  this.trials = [];
  this.isActive = true;
  this.responseWindowOpenedAt = 0;
  this.replayCount = 0;
  this.staircaseEngine.reset(this.config.baseFreq);
  this.minCentsAchieved = this.staircaseEngine.getState().centsDifference;
}
```

**호출 지점** — `app/training.tsx`의 답변 대기 타이머에서 호출 (4-2의 `runPlaybackSequence` 참조)

```tsx
addTimer(() => {
  setActiveSound('none');
  sessionManager.openResponseWindow();   // ← 추가
  setGameState('waiting');
}, (duration + gap + duration) * 1000);
```

> **데이터 영향:** 이 변경으로 기존 데이터와 새 데이터의 반응 시간 의미가 달라집니다.
> 누적 평균을 비교할 때 섞이지 않게 해야 합니다.

---

### 4-4. 수정: 톤 엔벨로프 서스테인 복구 (P0-4)

**문제.** Release 램프의 **종점**이 `duration - RELEASE_TIME`이었습니다.
Web Audio의 램프는 직전 자동화 이벤트 시점부터 시작하므로,
**0.05s → 0.95s 구간 내내** 0.4에서 0.001로 약 52dB 감쇠했습니다. 서스테인 구간이 없었습니다.

**파일 1:** `src/constants/theme.ts` — 게인 상수 추가

전
```ts
/** Gain Envelope 공격 시간 (초) */
ATTACK_TIME: 0.05,
/** Gain Envelope 릴리스 시간 (초) */
RELEASE_TIME: 0.05,
/** 포먼트 합성 F1 주파수 */
FORMANT_F1: 800,
```

후
```ts
/** Gain Envelope 공격 시간 (초) */
ATTACK_TIME: 0.05,
/** Gain Envelope 릴리스 시간 (초) */
RELEASE_TIME: 0.05,
/** 순수 파형 모드 서스테인 게인 */
PEAK_GAIN_WAVE: 0.4,
/** 포먼트 합성 모드 서스테인 게인 */
PEAK_GAIN_VOICE: 0.3,
/** 포먼트 합성 F1 주파수 */
FORMANT_F1: 800,
```

**파일 2:** `src/audio/AudioEngine.ts` — 구간 계산 헬퍼 신설

후 (신규 추가)
```ts
/**
 * Attack → Sustain → Release 엔벨로프의 구간 경계를 계산합니다.
 *
 * `releaseStart` 앵커가 없으면 Attack 종료 시점부터 Release 목표 시점까지
 * 램프가 이어져 톤 전체가 감쇠합니다. 서스테인 구간을 보장하기 위한 계산입니다.
 */
private envelopeTimes(
  startTime: number,
  duration: number,
): { attackEnd: number; releaseStart: number; end: number } {
  const end = startTime + duration;
  const attackEnd = Math.min(startTime + AUDIO.ATTACK_TIME, end);
  // duration이 Attack+Release보다 짧아도 순서가 뒤집히지 않도록 보정
  const releaseStart = Math.max(attackEnd, end - AUDIO.RELEASE_TIME);

  return { attackEnd, releaseStart, end };
}
```

**sine 모드 (`playSineTone`)**

전
```ts
// Envelope: 부드러운 Attack/Release (클릭/팝 방지)
gain.gain.setValueAtTime(0.001, startTime);
gain.gain.exponentialRampToValueAtTime(0.4, startTime + AUDIO.ATTACK_TIME);
gain.gain.exponentialRampToValueAtTime(
  0.001,
  startTime + duration - AUDIO.RELEASE_TIME,
);

osc.connect(gain);
gain.connect(ctx.destination);

osc.start(startTime);
osc.stop(startTime + duration);
```

후
```ts
const { attackEnd, releaseStart, end } = this.envelopeTimes(
  startTime,
  duration,
);
const peak = AUDIO.PEAK_GAIN_WAVE;

// Attack
gain.gain.setValueAtTime(0.001, startTime);
gain.gain.exponentialRampToValueAtTime(peak, attackEnd);
// Sustain — Release 시작점을 앵커로 고정해야 이 구간이 평탄하게 유지됨
gain.gain.setValueAtTime(peak, releaseStart);
// Release
gain.gain.exponentialRampToValueAtTime(0.001, end);

osc.connect(gain);
gain.connect(ctx.destination);

osc.start(startTime);
osc.stop(end);
```

**voice 모드 (`playVoiceTone`)**

전
```ts
// Envelope: linearRamp으로 Attack/Release
gain.gain.setValueAtTime(0.001, startTime);
gain.gain.linearRampToValueAtTime(0.3, startTime + 0.1);
gain.gain.linearRampToValueAtTime(
  0.001,
  startTime + duration - AUDIO.RELEASE_TIME,
);
...
osc.start(startTime);
osc.stop(startTime + duration);
```

후
```ts
const { releaseStart, end } = this.envelopeTimes(startTime, duration);
const peak = AUDIO.PEAK_GAIN_VOICE;
// voice 모드는 sine보다 완만한 Attack(0.1s) 사용
const attackEnd = Math.min(startTime + 0.1, releaseStart);

// Envelope: Attack → Sustain → Release (linearRamp)
gain.gain.setValueAtTime(0.001, startTime);
gain.gain.linearRampToValueAtTime(peak, attackEnd);
gain.gain.setValueAtTime(peak, releaseStart);
gain.gain.linearRampToValueAtTime(0.001, end);
...
osc.start(startTime);
osc.stop(end);
```

**변화 요약**

```
전:  ▁█▇▆▅▄▃▂▁▁▁▁     0.05s부터 0.95s까지 계속 감쇠 (서스테인 없음)
후:  ▁▄█████████▄▁     Attack 50ms → Sustain 900ms → Release 50ms
```

---

### 4-5. 문서 갱신 — `docs/앱_사용법.md`

동작이 바뀐 부분을 반영했습니다.

| 절 | 변경 |
|----|------|
| 2. 훈련 화면 구성 | "다시 듣기" / "다음 소리 듣기" 행 추가 + **버튼 상태별 문구 표** 신설 |
| 3. 기본 사용 순서 | 4번에 다시 듣기 안내, 6번에 "한 문제에 한 번만" 명시 |
| 5. 통계 | "평균 반응 시간" 정의에 "소리 듣는 2.5초는 포함되지 않습니다" 추가 |
| FAQ | "높음/낮음 버튼이 안 눌려요" 답변에 세 번째 경우(이미 답한 뒤) 추가 |
| FAQ | **"다시 듣기를 누르면 문제가 바뀌나요?"** 항목 신설 |

---

### 4-6. 지시 4의 검증

```
npx tsc --noEmit   →  오류 없음
npx eslint (변경 파일)  →  2건 (모두 기존 `useRef(new X()).current` 패턴, 이번 변경과 무관)
```

순수 모듈을 임시로 컴파일해 Node에서 실행 → **11 pass, 0 fail**
(방향 유지 / replayCount 초기화 / RT에 재생시간 미포함 / 서스테인 900ms 등)

P0-1은 UI 상태 게이트라 이 스크립트로 검증하지 않고 코드 경로 추적으로 확인했습니다.

### 4-7. 지시 4의 부수 변경 (의도치 않음)

`npx expo lint`를 처음 실행할 때 **`eslint.config.js`가 자동 생성**됐습니다.
이 프로젝트에는 ESLint 설정 파일이 없어서 lint가 한 번도 실행된 적이 없었습니다.

```js
// https://docs.expo.dev/guides/using-eslint/
const { defineConfig } = require('eslint/config');
const expoConfig = require("eslint-config-expo/flat");

module.exports = defineConfig([
  expoConfig,
  {
    ignores: ["dist/*"],
  }
]);
```

Expo 기본값 그대로입니다. 지우면 `npm run lint`가 동작하지 않아 남겨 두었고,
사용자에게 보고했습니다. **원치 않으면 삭제 가능합니다.**

### 4-8. 지시 4의 산출물

`reports/p0-fixes-2026-08-07.md` (신규)

---

## 지시 5 — "회귀 테스트가 무슨 의미?"

용어 설명 요청. **코드·문서 변경 없음.** 대화로만 답변했습니다.

요지: "회귀"는 고쳤던 것이 나중에 다시 원래대로 돌아가는 것.
P0 4건은 앱이 정상 동작하는 것처럼 보이면서 **저장되는 숫자만 조용히 틀리는** 종류라,
사람 눈으로는 재발을 알아채기 어렵다는 점을 설명했습니다.

---

## 지시 6 — "응 테스트 붙여줘"

### 6-1. 선행 작업: 상태 머신 분리

P0-1은 `app/training.tsx`의 `gameState` 게이트라, 그대로 두면
컴포넌트 렌더링 테스트(Skia·Reanimated·expo-router·audio-api 목 필요)를 거쳐야만 검증할 수 있었습니다.

규칙만 순수 함수로 떼어냈습니다.

**신규 파일:** `src/training/trainingFlow.ts`

```ts
export type GameState = 'idle' | 'playing' | 'waiting' | 'answered';

/**
 * 답변을 받을 수 있는 상태인가.
 *
 * 'waiting'(재생 완료·미답변)일 때만 true.
 * 'answered'를 포함시키면 같은 자극에 여러 번 답할 수 있게 되므로 절대 넓히지 말 것.
 */
export function canSubmitAnswer(state: GameState): boolean {
  return state === 'waiting';
}

/**
 * 재생 버튼이 '다시 듣기'(같은 문제 재생)로 동작하는 상태인가.
 *
 * false면 '새 문제'(방향 재추첨)로 동작합니다.
 */
export function isReplayPress(state: GameState): boolean {
  return state === 'waiting';
}

/** 재생 버튼을 누를 수 있는 상태인가. (재생 중에는 막는다) */
export function canPressPlay(state: GameState): boolean {
  return state !== 'playing';
}

export const PLAY_BUTTON_LABEL: Record<GameState, string> = {
  idle: '▶️ 소리 듣기 (A → B)',
  playing: '🔊 소리 감상 중...',
  waiting: '🔁 다시 듣기 (A → B)',
  answered: '▶️ 다음 소리 듣기 (A → B)',
};
```

**`app/training.tsx` 변경**

전
```tsx
import { SessionManager, SessionMode } from '../src/training/SessionManager';
...
type GameState = 'idle' | 'playing' | 'waiting' | 'answered';
```

후
```tsx
import { SessionManager, SessionMode } from '../src/training/SessionManager';
import {
  GameState,
  PLAY_BUTTON_LABEL,
  canPressPlay,
  canSubmitAnswer,
  isReplayPress,
} from '../src/training/trainingFlow';
```
(로컬 `type GameState` 선언 삭제)

| 위치 | 전 | 후 |
|------|-----|-----|
| `handleNextRound` 가드 | `if (gameState === 'playing') return;` | `if (!canPressPlay(gameState)) return;` |
| `handleReplay` 가드 | `if (gameState !== 'waiting') return;` | `if (!isReplayPress(gameState)) return;` |
| `handlePlayPress` | `gameState === 'waiting' ? …` | `isReplayPress(gameState) ? …` |
| `handleAnswer` 가드 | `if (gameState !== 'waiting') return;` | `if (!canSubmitAnswer(gameState)) return;` |
| 재생 버튼 `disabled` | `gameState === 'playing'` | `!canPressPlay(gameState)` |
| `AnswerButtons disabled` | `gameState !== 'waiting'` | `!canSubmitAnswer(gameState)` |

**동작은 완전히 동일합니다.** 판정 로직이 한 곳에 모였을 뿐입니다.

---

### 6-2. 테스트 파일 5개 (신규)

| 파일 | 테스트 수 | 방어 대상 |
|------|----------|-----------|
| `src/training/__tests__/trainingFlow.test.ts` | 17 | **P0-1, P0-2** |
| `src/training/__tests__/SessionManager.test.ts` | 12 | **P0-2, P0-3** + 세션 집계 |
| `src/audio/__tests__/AudioEngine.test.ts` | 16 | **P0-4** |
| `src/training/__tests__/StaircaseEngine.test.ts` | 17 | 계단법 규칙 고정 |
| `src/audio/__tests__/pitchUtils.test.ts` | 24 | 명세서 §5.2 검증 수치 고정 |
| **합계** | **86** | |

**AudioEngine 테스트 방식.** `react-native-audio-api`를 목으로 대체해,
`GainNode`에 **예약된 자동화 이벤트 목록**을 검사합니다. 실제 소리는 내지 않습니다.

```
wave 모드 1.0초 톤에 예약되는 이벤트:
  setValueAtTime(0.001, 0.00)     Attack 시작
  expRamp(0.4,     0.05)          Attack 종료
  setValueAtTime(0.4,   0.95)     Sustain 앵커  ← 이게 없으면 P0-4 재발
  expRamp(0.001,   1.00)          Release 종료
```

---

### 6-3. jest 환경 문제 2건 (이 프로젝트에서 jest 최초 실행)

#### (1) `Cannot find module 'expo-modules-core'`

`expo-modules-core@57.0.2`가 `node_modules/expo/node_modules/` 아래에 **중첩 설치**돼
`jest-expo`의 setup이 해석하지 못했습니다.

#### (2) `Haste module naming collision: rn-hear-1`

`files/package.json`이 루트 `package.json`과 이름이 같아 충돌했습니다.

**파일:** `package.json`

전
```json
"scripts": {
  ...
  "test": "jest --watchAll",
  "lint": "expo lint"
},
"jest": {
  "preset": "jest-expo/android"
},
```

후
```json
"scripts": {
  ...
  "test": "jest --watchAll",
  "test:ci": "jest --ci",
  "lint": "expo lint"
},
"jest": {
  "preset": "jest-expo/android",
  "modulePathIgnorePatterns": [
    "<rootDir>/files/"
  ],
  "moduleDirectories": [
    "node_modules",
    "<rootDir>/node_modules/expo/node_modules"
  ]
},
```

- 의존성은 **추가하지 않았습니다.** 해석 경로만 넓혔습니다.
- 기존 `"test": "jest --watchAll"`은 감시 모드라 CI에서 멈추지만,
  기존 스크립트를 건드리지 않고 `test:ci`를 별도로 추가했습니다.

**파일:** `tsconfig.json` — `tsc`가 jest 전역(`describe`, `it`, `expect`)을 모름

전
```json
"compilerOptions": {
  "strict": true,
  "paths": {
    "@/*": ["./src/*"]
  }
},
```

후
```json
"compilerOptions": {
  "strict": true,
  "types": ["jest"],
  "paths": {
    "@/*": ["./src/*"]
  }
},
```

---

### 6-4. 테스트가 실제로 재발을 잡는지 확인

작성한 테스트를 믿으려면 "고친 걸 되돌렸을 때 실패하는가"를 봐야 합니다.
4건 각각을 **일시적으로 되돌려** 확인했고, **모두 원복했습니다.**

| 되돌린 내용 | 실패한 테스트 |
|------------|--------------|
| `canSubmitAnswer`에 `'answered'` 추가 | `'answered'를 허용하면 안 된다` / `'answered'에서는 답변을 받지 않는다` / `답변을 받는 상태는 정확히 하나뿐이다` |
| `isReplayPress`를 항상 `false`로 | `'waiting'에서는 다시 듣기로 동작한다` / `다시 듣기로 동작하는 상태는 정확히 하나뿐이다` / `답변을 받는 상태와 다시 듣기 상태가 일치한다` |
| `prepareRound`에서 다시 시계를 잡도록 | `prepareRound는 측정을 시작하지 않는다` |
| 서스테인 앵커 제거 + 램프 종점을 `releaseStart`로 | `Release 시작점에 서스테인 앵커가 있다` / `마지막 램프가 톤 끝에서 끝난다` / `오프셋만큼 뒤로 밀어 예약한다` |

---

### 6-5. 이 과정에서 발견한 **무력한 테스트 2건**을 수정

되돌림 검증 중, 처음 작성한 테스트 2개가 **버그 상태에서도 통과**한다는 것을 발견했습니다.

#### (1) P0-3 — "자극 재생 시간이 포함되지 않는다"

문제: 테스트 흐름이 `prepareRound → 2500ms 경과 → openResponseWindow → 답변`이라,
`prepareRound`가 시계를 잡아도 `openResponseWindow`가 덮어써서 통과했습니다.

**전 (무력)**
```ts
it('측정 창이 열리기 전 답변은 0으로 기록한다', () => {
  const clock = useFakeClock();
  const sm = new SessionManager({ baseFreq: 440 });
  sm.startSession();

  sm.prepareRound();
  clock.advance(1200);
  sm.submitAnswer(true); // openResponseWindow 없이

  expect(sm.endSession().trials[0].reactionTimeMs).toBe(0);
});
```

**후 (의도를 명시 + 테스트 1개 추가)**
```ts
it('prepareRound는 측정을 시작하지 않는다', () => {
  // 이 단언이 이 결함의 핵심 방어선이다.
  // prepareRound가 시계를 잡으면(수정 전 동작) 아래 값이 1200이 되고,
  // 실제 앱에서는 자극 재생 2500ms가 반응 시간에 그대로 섞여 들어간다.
  const clock = useFakeClock();
  const sm = new SessionManager({ baseFreq: 440 });
  sm.startSession();

  sm.prepareRound();
  clock.advance(1200);
  sm.submitAnswer(true); // openResponseWindow 없이

  expect(sm.endSession().trials[0].reactionTimeMs).toBe(0);
});

it('답변 후에는 측정 창이 닫힌다', () => {
  // 창이 열린 채 남으면 다음 라운드의 반응 시간이 이전 라운드 시점부터 잰다
  const clock = useFakeClock();
  const sm = new SessionManager({ baseFreq: 440 });
  sm.startSession();

  sm.prepareRound();
  sm.openResponseWindow();
  clock.advance(300);
  sm.submitAnswer(true);

  clock.advance(5000); // 다음 문제를 누르기까지 한참 쉼
  sm.prepareRound();
  clock.advance(2500); // 재생
  sm.submitAnswer(true); // openResponseWindow를 건너뛴 상황

  expect(sm.endSession().trials[1].reactionTimeMs).toBe(0);
});
```

> 같은 파일의 "자극 재생 시간(2500ms)이 포함되지 않는다" 테스트는 그대로 두었습니다.
> 정상 흐름의 값(700ms)을 고정하는 역할은 여전히 유효하지만,
> **재발 방어선은 위의 `prepareRound는 측정을 시작하지 않는다`입니다.**

#### (2) P0-4 — "서스테인 구간이 톤 길이의 80% 이상"

문제: `releaseStart`를 상수(`duration - RELEASE_TIME`)로 계산해서,
실제 예약 이벤트를 보지 않아 앵커가 없어도 통과했습니다.

**전 (무력)**
```ts
it('서스테인 구간이 톤 길이의 80% 이상이다', async () => {
  const events = await playOnce();
  const releaseStart = duration - AUDIO.RELEASE_TIME;   // ← 상수. 실제 스케줄과 무관

  const attackEnd = events.find((e) => e.value === peak)!.time;
  const sustain = releaseStart - attackEnd;

  expect(sustain).toBeGreaterThan(0);
  expect(sustain).toBeGreaterThanOrEqual(duration * 0.8);
});
```

**후 (실제 이벤트에서 도출 + 테스트 1개 추가)**
```ts
it('Release 직전까지 게인이 피크로 유지된다', async () => {
  const events = await playOnce();
  const releaseStart = duration - AUDIO.RELEASE_TIME;

  // Release 시작 시점에 유효한 마지막 자동화 값이 곧 그 구간의 게인이다.
  // 앵커가 없으면 이 값이 0에 가까운 감쇠 목표가 된다 (= P0-4 재발).
  const lastBeforeRelease = events
    .filter((e) => e.time <= releaseStart)
    .pop()!;

  expect(lastBeforeRelease.value).toBe(peak);
});

it('서스테인 구간이 톤 길이의 80% 이상이다', async () => {
  const events = await playOnce();

  // 피크에 도달하는 첫 이벤트 = Attack 종료
  const attackEnd = events.find((e) => e.value === peak)!.time;
  // 피크를 유지하는 마지막 이벤트 = Release 시작
  const releaseStart = events.filter((e) => e.value === peak).pop()!.time;
  const sustain = releaseStart - attackEnd;

  expect(sustain).toBeGreaterThan(0);
  expect(sustain).toBeGreaterThanOrEqual(duration * 0.8);
});
```

---

### 6-6. 작성 중 걸린 기술적 함정 (기록용)

**jest 목 팩토리 안에서 TypeScript 파라미터 프로퍼티 금지**

```ts
// ✗ 스위트 전체가 실행되지 않음
// ReferenceError: The module factory of `jest.mock()` is not allowed to
//                 reference any out-of-scope variables. Invalid variable access: name
class FakeAudioParam {
  constructor(private readonly name: string) {}
}

// ○ 평범한 할당
class FakeAudioParam {
  name: string;
  constructor(name: string) {
    this.name = name;
  }
}
```

`babel-plugin-jest-hoist`가 파라미터 프로퍼티의 `name`을 스코프 밖 변수 참조로 오인합니다.
해당 파일 상단에 주석으로 남겨 두었습니다.

---

### 6-7. 지시 6의 최종 검증

```
Test Suites: 5 passed, 5 total
Tests:       86 passed, 86 total
Time:        3.05 s

npx tsc --noEmit              →  오류 없음
npx eslint (신규 파일 전체)     →  오류·경고 없음
```

### 6-8. 지시 6의 산출물

`reports/p0-regression-tests-2026-08-07.md` (신규)

---

## 지시 7·8·9 — 진행 상황 확인

| 지시 | 대응 |
|------|------|
| "현재 어디까지 했어" | 진행 상황 보고. 변경 없음 |
| "지금 회귀테스트를 구현 중?" | 확인 답변. 변경 없음 |
| "네" | 지시 6 작업 계속 진행 |

---

## 지시 10 — "작업 기록을 md로"

이 문서(`reports/작업기록_2026-08-07.md`)를 생성했습니다.

---

## 지시 11 — "회귀테스트는 끝난 거야? 내가 테스트할 것은 없어?"

**답변 요지**

- 회귀 테스트는 코드로 덮을 수 있는 범위까지 완료. 단, **실제 소리**와 **화면 배선**은
  자동 테스트가 닿지 못해 사람 확인이 필요함.
- P0-4는 오디오 **스케줄만** 검증했으므로, 서스테인 앵커 추가로 인한
  **끝단 클릭/팝 잡음**이 새로 생겼을 가능성이 남아 있음.

**산출물:** `docs/실기기_확인_체크리스트.md` (신규) — A~F 6개 항목의 확인 절차

---

## 지시 12 — 사용자 확인 결과 보고: "에뮬레이터에서는 문제없이 동작했어"

**보고 내용:** Android 에뮬레이터에서 이상 없이 동작.

**해석 및 처리**

| 체크리스트 항목 | 상태 | 근거 |
|----------------|------|------|
| B. 답변 중복 제출 차단 | ✅ 확인됨 | 로직 동작은 에뮬레이터에서도 실기기와 동일 |
| C. 다시 듣기 분기 | ✅ 확인됨 | 〃 |
| D. 반응 시간 | ✅ 확인됨 | 〃 |
| E. 화면 배선 (버튼 활성 타이밍) | ✅ 확인됨 | 〃 |
| **A. 소리 (지속 길이·잡음)** | ⚠️ **미확인 유지** | 아래 |
| F. 기존 데이터 초기화 | ⬜ 결정 대기 | 사용자 판단 사항 |

**A 항목을 확인됨으로 처리하지 않은 이유**

에뮬레이터 오디오는 호스트 PC의 오디오 스택을 한 번 더 거치며 리샘플링·버퍼링이 추가됩니다.
그 과정에서 **끝단 클릭/팝 잡음이 가려지는 경우가 흔합니다.**
에뮬레이터에서 안 들렸다는 사실이 실기기에서 안 난다는 근거가 되지 못합니다.

P0-4 수정은 Release 시작점에 `setValueAtTime` 앵커를 새로 넣은 변경이라,
**이번 작업이 새 잡음을 유발했을 가능성**이 구조적으로 존재합니다.
따라서 실기기 + 이어폰 확인 전까지는 미확인으로 둡니다.

`docs/실기기_확인_체크리스트.md` 상단에 확인 진행 상황 표를 추가하고
위 구분을 기록했습니다.

---

## 지시 13 — 문서 운영 규칙 결정: "단계별로 파일 분리(B안)"

작업 기록 파일이 1,141줄까지 늘어난 상태에서, 앞으로의 기록 방식을 정했습니다.

**제시한 선택지**

| 안 | 내용 |
|----|------|
| A | 하나의 파일에 계속 추가 |
| **B** | **단계별로 파일 분리** ← 선택됨 |
| C | 날짜별로 분리 |
| D | 요청할 때만 기록 |

**적용**

- `reports/작업기록_2026-08-07.md` → **`reports/작업기록_1단계_P0수정.md`** 로 이름 변경
- 이 문서를 1단계에서 **마감**
- 2단계 지시부터는 `reports/작업기록_2단계_측정타당도.md`에 기록
- 외부 참조 없음을 확인 후 변경 (`grep` 결과 자기 참조만 존재)

---

## 전체 변경 파일 목록

### 앱 코드 (수정)

| 파일 | 지시 | 내용 |
|------|------|------|
| `app/training.tsx` | 4, 6 | 재생/다시듣기 분리, 답변 게이트 축소, `trainingFlow` 사용 |
| `src/audio/AudioEngine.ts` | 4 | `envelopeTimes()` 신설, 서스테인 복구 (sine·voice) |
| `src/training/SessionManager.ts` | 4 | 반응 시간 측정 시점 이동, `openResponseWindow`/`countReplay`, `replayCount` |
| `src/constants/theme.ts` | 4 | `PEAK_GAIN_WAVE` / `PEAK_GAIN_VOICE` 상수 |

### 앱 코드 (신규)

| 파일 | 지시 |
|------|------|
| `src/training/trainingFlow.ts` | 6 |

### 테스트 (신규, 전부 지시 6)

```
src/training/__tests__/trainingFlow.test.ts
src/training/__tests__/SessionManager.test.ts
src/training/__tests__/StaircaseEngine.test.ts
src/audio/__tests__/AudioEngine.test.ts
src/audio/__tests__/pitchUtils.test.ts
```

### 설정

| 파일 | 지시 | 내용 |
|------|------|------|
| `package.json` | 6 | jest `modulePathIgnorePatterns`·`moduleDirectories`, `test:ci` 스크립트 |
| `tsconfig.json` | 6 | `"types": ["jest"]` |
| `eslint.config.js` | 4 | **자동 생성됨** (`expo lint` 최초 실행 부수 효과) |

### 문서

| 파일 | 지시 | 신규/수정 |
|------|------|----------|
| `docs/앱_개선_제안서.md` | 1 | 신규 (기준점 이전) |
| `docs/앱_개선_제안서_쉬운버전.md` | 3 | 신규 (기준점) |
| `docs/앱_사용법.md` | 4 | 수정 |
| `reports/p0-fixes-2026-08-07.md` | 4 | 신규 |
| `reports/p0-regression-tests-2026-08-07.md` | 6 | 신규 |
| `reports/작업기록_2026-08-07.md` | 10 | 신규 (이 문서) |

### 이 작업과 무관한 변경 (세션 시작 전부터 있던 것)

```
 M docs/기준음_선택과_주파수_클램프_계획.md
?? reports/reference-pitch-wording-correction-2026-08-07.md
```

---

## 현재 검증 상태

| 항목 | 결과 |
|------|------|
| `npx tsc --noEmit` | 오류 없음 |
| `npx jest --ci` | 5 suites / **86 tests 전부 통과** (3.05s) |
| `npx eslint` (신규·변경 파일) | 오류·경고 없음 |
| 되돌림 검증 | P0 4건 모두 테스트가 실패로 잡아냄 |

---

## 아직 하지 않은 것

이 부분이 다음 작업에 넘어갑니다.

### 검증 공백

- **실기기 청감 확인 없음.** P0-4는 오디오 스케줄만 검증했습니다.
  Android 실기기에서 톤이 1초 유지되는지, 서스테인 앵커 때문에 클릭/팝이 생기지 않는지는
  **직접 들어봐야 합니다.**
- **`app/training.tsx`의 배선 미검증.** 규칙(`trainingFlow`)과 계측(`SessionManager`)은 덮었지만,
  `openResponseWindow()`가 B 재생 종료 타이머에서 실제로 호출되는지는 테스트가 없습니다.
  컴포넌트 테스트가 필요하고 Skia·Reanimated·expo-router 목이 선행돼야 합니다.
- **`TrainingStorage` 테스트 없음.** AsyncStorage 목이 필요합니다.
- **CI 없음.** `tsc` + `eslint` + `jest`를 자동 실행하는 장치가 없어 사람이 기억해서 돌려야 합니다.

### 데이터 처리 미결

이번 수정 이전에 저장된 세션은 신뢰할 수 없습니다.

| 항목 | 이전 데이터의 문제 |
|------|-------------------|
| `totalTrials` · `correctCount` · `accuracy` | 중복 답변으로 부풀려졌을 수 있음 (P0-1) |
| `minCentsAchieved` | 중복 답변으로 인위적 하강, 문제 회피로 인한 편향 (P0-1, P0-2) |
| `reactionTimeMs` | 재생 시간 2500ms 포함 (P0-3) |
| 전반 | 자극 길이가 의도보다 짧아 난이도 과대 (P0-4) |

**아직 초기화하지 않았습니다.** 설정 → 훈련 데이터 전체 초기화를 하시거나,
분석 시 이 커밋 이전 세션을 제외해야 합니다.

### 미결 판단 사항

- `eslint.config.js`를 남길지 삭제할지 (현재 남겨 둠)
- `"test": "jest --watchAll"`을 CI 친화적으로 바꿀지 (현재 그대로 두고 `test:ci` 추가)
- 커밋 여부 — **아직 커밋하지 않았습니다.** 전부 워킹 트리에만 있습니다.

---

## 다음 단계 (제안서 2단계)

`docs/앱_개선_제안서.md` §3 기준입니다.

| # | 항목 | 내용 |

|---|------|------|
| P1-1 | 반전 기반 역치 산출 | 현재 대표 지표 `minCentsAchieved`는 극단값이라 운에 좌우됨 |
| P1-3 | 시작 난이도 상향 + 가변 스텝 | 50 cent 시작은 초심자 이탈 요인 |
| P1-2 | 평가 모드 실제 분기 | 현재 라벨만 다르고 동작이 훈련과 동일, 평가 중에도 피드백 노출 |
| P0-5 | 오디오 클럭 스케줄링 | A→B 간격이 아직 `setTimeout` 의존 |
| P0-6 | `AppState` 인터럽션 처리 | 전화·백그라운드 전환 시 라운드가 계속 진행됨 |
| P1-7 | 표시용 연속 카운터 분리 | 현재 배지가 0/1만 반복 |

> `StaircaseEngine.test.ts`가 현재 규칙을 고정해 두었으므로,
> 위 작업에서 **깨지는 테스트가 곧 "무엇이 의도적으로 바뀌었는지"의 목록**이 됩니다.
> 깨진 테스트는 새 규칙에 맞게 갱신하되, **P0 4건을 지키는 단언은 그대로 통과해야 합니다.**

---

# 문서 마감

**이 문서는 1단계에서 종료합니다. 이후 내용은 추가하지 않습니다.**

2단계 지시부터는 `reports/작업기록_2단계_측정타당도.md`에 기록합니다.

## 1단계 최종 상태

| 항목 | 결과 |
|------|------|
| P0 결함 4건 수정 | 완료 |
| 회귀 테스트 | 86개, 전부 통과 |
| `tsc --noEmit` | 오류 없음 |
| `eslint` (신규·변경 파일) | 오류·경고 없음 |
| 되돌림 검증 | P0 4건 모두 테스트가 잡아냄 |
| 에뮬레이터 동작 확인 | 이상 없음 (B·C·D·E 항목) |

## 2단계로 넘기는 미결 사항

| # | 항목 | 성격 |
|---|------|------|
| 1 | **실기기 소리 확인 (A 항목)** | 사람만 가능. 서스테인 앵커로 인한 클릭/팝 잡음 위험이 미해소 |
| 2 | **기존 오염 데이터 처리 (F 항목)** | 사용자 결정 대기 — 초기화 vs. 분석 시 제외 |
| 3 | `app/training.tsx` 컴포넌트 테스트 | Skia·Reanimated·expo-router 목 선행 필요 |
| 4 | `TrainingStorage` 테스트 | AsyncStorage 목 선행 필요 |
| 5 | CI 구성 | `tsc` + `eslint` + `jest` 자동 실행 없음 |
| 6 | `eslint.config.js` 존치 여부 | 자동 생성된 파일. 현재 남겨 둠 |
| 7 | 커밋 | 미실행. 전부 워킹 트리에만 존재 |
