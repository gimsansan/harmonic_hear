# 미사용 코드·의존성 정리 결과

날짜: 2026-08-10  
계획: `docs/미사용코드_정리_계획.md`

## 한 일

| 항목 | 결과 |
|------|------|
| `src/audio/keyzoneUtils.ts` | 삭제됨 |
| `src/constants/theme.ts`의 `KEYZONE` | 삭제됨 |
| `files/package.json`, `package-lock.json`, `app.json` | 삭제됨 (`files/` 명세 MD는 유지) |
| `package.json` dependencies | `@react-navigation/bottom-tabs`, `expo-audio`, `expo-screen-orientation` 제거 |
| `app.json` plugins | `expo-audio` 제거 |
| `npm install` | 성공 (`up to date`, audited 1168 packages) |

## 확인

- `package-lock.json`에서 `expo-audio`, `@react-navigation/bottom-tabs`, `expo-screen-orientation` 문자열 검색 → 없음
- `package.json`에 `reset-project` / `ios` 스크립트 없음 (Android 스크립트만 유지)
- 유지한 의존성: `expo-font`, `expo-asset`, `expo-linking`, `@react-navigation/native`, `@react-navigation/elements`

## 성능 영향

- 미사용 패키지 제거로 설치 트리·`node_modules` 부담이 줄어듦 (앱 런타임 핫패스 변화는 없음)
- 죽은 TS 모듈 삭제는 번들 크기 영향 미미 (원래 import되지 않음)

## 남긴 것

- `files/하모니튠_통합_구현명세서.md`, `implementation_plan*.md` (문서 참조용)
- `docs/`, `reports/` 기존 문서
