# 용어 부가 안내 — 구현 결과

## 계획

우선순위 4곳만 반영:

1. 훈련 진행 문구(짧은 `역치`) 탭 설명
2. 통계 `최고 역치` 탭 설명
3. 결과 `—` 상태: 저장됨 vs 최소 차이 미계산 구분
4. 차트 빈 상태: “최소 차이가 나온 세션 2회”

## 한 일

- `THRESHOLD_TERM_HELP` 상수 추가 (`theme.ts`)
- `training.tsx`: 훈련 모드 진행 문구에 help 아이콘 + Alert
- `stats.tsx`: `최고 역치` 타일 탭 + help 아이콘 + Alert
- `result.tsx`: `—`일 때 “세션은 저장됐지만, 최소 차이는 아직 계산 전입니다” 추가
- `ThresholdChart.tsx`: 빈 상태 문구를 최소 차이 세션 기준으로 구체화

## 확인

- 코드 변경 위치·문구 반영 확인
- 실기기 탭/Alert·결과·차트 빈 상태는 앱에서 확인 필요
